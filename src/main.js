import { getNode, insertLast } from "kind-tiger";
import { insertLast } from "./../node_modules/kind-tiger/dom/insert";

const app = getNode("#app");

const template = ``;

insertLast(app, template);
